---
description: 本仓库是模仿 Ant Design 的 Tiptap 编辑器，UI 必须对齐 antd 5 观感
alwaysApply: true
---

# 产品定位

`@allahbin/tiptap` 是一套 **模仿 Ant Design 的 Tiptap 编辑器**，不是 Notion 皮肤，也不是官方 Tiptap UI 皮肤。

- 公文编辑器：`ATiptap`
- 块编辑器：`ANotion`（交互可参考 Notion，**视觉必须是 antd**）
- 未接入 `antd` 包时，也要用 CSS token **模仿** Ant Design 5 默认主题，不要引入暖灰 Notion 色或蓝灰胶囊色

## 必须遵守

- 控件、下拉、划词栏、斜杠菜单、拖拽句柄：按 antd Button / Dropdown / Menu
- 不要为了「更像 Notion」改回 `#37352f`、`#efeee9`、`#acbbd4`
- 不要把收费 Tiptap UI / Figma 菜单样式原样搬进来

## Token（antd 5 默认）

```css
--atiptap-color-primary: #1677ff;
--atiptap-color-primary-hover: #4096ff;
--atiptap-color-primary-bg: #e6f4ff;
--atiptap-color-text: rgba(0, 0, 0, 0.88);
--atiptap-color-text-tertiary: rgba(0, 0, 0, 0.45);
--atiptap-color-border: #d9d9d9;
--atiptap-border-radius: 6px; /* 菜单 8px，选项 4px */
```

阴影用 antd Dropdown：`0 6px 16px 0 rgba(0,0,0,0.08), 0 3px 6px -4px rgba(0,0,0,0.12), 0 9px 28px 8px rgba(0,0,0,0.05)`。
字体栈用 antd：`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif`。
